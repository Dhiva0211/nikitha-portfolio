const cookieName = 'fdp';
const timeSessionToken = '1h';
const timeRefreshToken = '1w';
const maxAgeCookie = 3600 * 24 * 7;

export default cookieName;
export { timeSessionToken, timeRefreshToken, maxAgeCookie };
